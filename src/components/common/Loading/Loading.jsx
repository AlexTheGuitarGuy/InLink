import React from 'react';
import loading from '../../../assets/loading.jpg';

const Loading = () => {
  return <span className="flex justify-center items-center h-screen"><img src={loading} alt="loading..." /></span>
}

export default Loading